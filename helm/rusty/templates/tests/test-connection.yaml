apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rusty.fullname" . }}-test-connection"
  labels:
    {{- include "rusty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rusty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
