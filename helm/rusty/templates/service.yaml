apiVersion: v1
kind: Service
metadata:
  name: {{ include "rusty.fullname" . }}
  labels:
    {{- include "rusty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rusty.selectorLabels" . | nindent 4 }}
